# ── Rust build ────────────────────────────────────────────────
/target/
**/*.rs.bk

# ── Secrets: real account tokens & config (NEVER commit) ──────
# The live config holds OAuth access/refresh tokens for real accounts.
# Default path is ~/.config/teamclaude.json (outside the repo); these
# patterns fail closed against any copy placed inside it.
teamclaude.json
teamclaude*.json
.teamclaude.json
config.json
config.local.json
*.local.json
config/

# ── Secrets: MITM CA material & any key/cert ─────────────────
*.pem
*.key
*.crt
*.cer
*.der
*.p12
*.pfx
ca.*

# ── Secrets: environment files ───────────────────────────────
.env
.env.*
!.env.example

# ── Logs & runtime captures ──────────────────────────────────
*.log
# `*.log` alone does not match tracing-appender's rotated shape, which
# suffixes rather than extends the name: `teamclaude-rs.log.2026-08-08` ends
# in the date, not `.log`, and account emails + request paths live in it. The
# trailing dot below is deliberate: `teamclaude-rs.log.*` matches only names
# that continue PAST `.log` (the rotated date suffixes) and leaves the bare
# `teamclaude-rs.log` to the pre-existing `*.log` rule above — a bare `*`
# here would also swallow an unrelated future source file like
# `teamclaude-rs.logic.rs`, which must never be silently ignored.
teamclaude-rs.log.*
*.jsonl
tcr-capture*
/tmp-capture/

# ── Internal dev scaffolding (Henry swarm state, not public) ─
docs/plans/
*-swarm-state.md
*-bridge-*.md
*-handoff.md

# ── Editor / IDE ─────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
*~

# ── OS cruft ─────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── macOS app (TcrBar) build output ───────────────────────────
apps/macos/.build/
apps/macos/build/
